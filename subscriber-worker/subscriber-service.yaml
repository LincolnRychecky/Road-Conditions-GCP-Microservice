kind: Service
apiVersion: v1
metadata:
  name: subscriber-svc
  labels:
   app: subscriberManager
spec:
  spec:
  #type: NodePort
  selector:
    app: subscriberManager
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
---